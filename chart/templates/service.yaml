apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsi-aggregator-proxy.fullname" . }}
  labels:
    {{- include "nsi-aggregator-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nsi-aggregator-proxy.selectorLabels" . | nindent 4 }}
