apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsi-aggregator-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "nsi-aggregator-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsi-aggregator-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
